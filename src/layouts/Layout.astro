---
import { Font } from "astro:assets";
import { Card, CardTitle } from "../components/card";
import LayoutNavigation from "../components/compositions/layout-navigation.tsx";
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  publishedAt?: string;
  updatedAt?: string;
}

const { title, description, publishedAt, updatedAt } = Astro.props;

const jsonLD = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  headline: title,
  datePublished: publishedAt,
  dateModified: updatedAt,
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />

    <Font cssVariable="--font-fira-code" />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />

    <title>{title ?? "Ketela's Blog"}</title>

    {title && <meta name="og:title" content={title} />}
    {description && <meta name="description" content={description} />}

    <meta name="generator" content={Astro.generator} />
    <meta name="robots" content="index, follow" />

    <script
      type="application/ld+json"
      is:inline
      set:html={JSON.stringify(jsonLD)}
    />
  </head>
  <body class="bg-dark-bg text-dark-text-primary px-8 py-8 font-mono md:px-72">
    <header class="mb-8 grid grid-cols-12 gap-4">
      <Card className="col-span-10">
        <CardTitle>
          <h1>Navigation</h1>
        </CardTitle>

        <LayoutNavigation client:load />
      </Card>
      <Card className="col-span-2" />
    </header>

    <div class="grid grid-cols-3 gap-4">
      <aside class="col-span-3 flex flex-col gap-8 md:col-span-1">
        <Card>
          <CardTitle>
            <h1>About Me</h1>
          </CardTitle>

          <div class="flex flex-col gap-2">
            <pre
              aria-hidden="true"
              class="text-dark-text-primary inline-block text-left text-xs leading-none whitespace-pre">
██╗  ██╗███████╗████████╗███████╗██╗      █████╗
██║ ██╔╝██╔════╝╚══██╔══╝██╔════╝██║     ██╔══██╗
█████╔╝ █████╗     ██║   █████╗  ██║     ███████║
██╔═██╗ ██╔══╝     ██║   ██╔══╝  ██║     ██╔══██║
██║  ██╗███████╗   ██║   ███████╗███████╗██║  ██║
╚═╝  ╚═╝╚══════╝   ╚═╝   ╚══════╝╚══════╝╚═╝  ╚═╝
            </pre>

            <p>
              Hi! My name is{" "}
              <span class="text-dark-text-primary underline">Ketela</span>,
              welcome to my blog. You can read my random yappers here. Enjoy :D
            </p>
          </div>
        </Card>

        <Card>
          <CardTitle>
            <h1>Free Space</h1>
          </CardTitle>

          <p>
            Don't know what to put here yet but I don't like how empty it is.
          </p>
        </Card>
      </aside>

      <main class="col-span-3 flex flex-col gap-8 md:col-span-2">
        <slot />
      </main>
    </div>
  </body>
</html>
